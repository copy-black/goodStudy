# 写个server端口 63040
#server:
#  servlet:
#    context-path: /content
#  port: 63040

#微服务配置
spring:
  application:
    name: goodstudy-content-api
  cloud:
    nacos:
      server-addr: 192.168.101.65:8848
      discovery:
        namespace: goodstudy_dev
        group: goodstudy-project
      config:
        namespace: goodstudy_dev
        group: goodstudy-project
        file-extension: yaml
        refresh-enabled: true
        extension-configs:
          - data-id: goodstudy-content-service-${spring.profiles.active}.yaml
            group: goodstudy-project
            refresh: true
        shared-configs:
          - data-id: swagger-${spring.profiles.active}.yaml
            group: goodstudy-plus-common
            refresh: true
          - data-id: logging-${spring.profiles.active}.yaml
            group: goodstudy-plus-common
            refresh: true
          - data-id: freemarker-config-${spring.profiles.active}.yaml
            group: goodstudy-plus-common
            refresh: true
  profiles:
    active: dev


#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/gs_content?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#    username: root
#    password: root


## 日志文件配置路径
#logging:
#  config: classpath:log4j2-dev.xml
#
## Swagger
#swagger:
#  enabled: true
#  title: goodstudy-内容服务
#  description: 内容服务接口文档
#  version: 1.0.0
#  contact:
#    name: goodstudy
#    url:
