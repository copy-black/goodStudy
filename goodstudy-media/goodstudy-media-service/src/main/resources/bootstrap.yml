spring:
  application:
    name: goodstudy-media-service
  cloud:
    nacos:
      server-addr: 192.168.101.65:8848
      discovery:
        namespace: goodstudy_dev
        group: goodstudy-project
      config:
        namespace: goodstudy_dev
        group: goodstudy-project
        file-extension: yaml
        refresh-enabled: true
      shared-configs:
        - data-id: logging-${spring.profiles.active}.yaml
          group: goodstudy-plus-common
          refresh: true

#profiles默认为dev
  profiles:
    active: dev



